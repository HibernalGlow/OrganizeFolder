rules:
  - name: "Remove empty directories"
    locations: ${locations}
    subfolders: true
    targets: dirs
    filters:
      - empty
    actions:
      - echo: "Found empty directory: {path}"
      - delete